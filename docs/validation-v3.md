# Meme Studio v3 验收记录

日期：2026-09-10。验证流程运行与数据传递，不代替玩法质量判断。

## 自动验证

`npm test`：19 项通过。两个新 Skill 通过 quick_validate；Skill 文件摘要一致。覆盖阶段隔离、确认门槛、全站并发 3、排队不计时、超时检查点恢复、无有效记录停止、子进程清理、取消、改编重试复用研究、纠正后失效、素材引用与 Range、七项导出、历史记录不改写。

## 真实 CLI 链路

首次研究因账号额度用尽中断。用户重置后，复用已完成梗理解重新研究；原失败和取消记录保留，没有伪装为首次成功。

| Meme | 有效机制 | 研究耗时（含退出清理） | 实际查询 | 改编调用 | 选题 | 素材 |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| 厉飞雨 | 2 | 300.039 秒 | 12 | 1 | 1 | 0 个登记文件 |
| 广东人吃福建人 | 3 | 300.044 秒 | 9 | 1 | 1 | 2 个登记文件 |

两次研究都在 300 秒达到时限，恢复本次有效检查点后进入一次改编。无额外总结、展开或审核调用。实际改编 prompt 中的 confirmed、assets、research、count 已与保存数据逐项比较一致；工作目录只含改编 Skill。

厉飞雨未获得可用音视频，结果写明待补；页面没有虚假播放器。广东登记两张原图，选题实际引用一张（440 × 302），另一张说明不使用的原因。没有把远程链接冒充本地素材。

## 页面与兼容

在隔离页面完成输入、确认、七项结果展示和研究折叠区检查。3 秒 WAV 与 8 秒 MP4 测试文件能够播放并跳转进度；实际广东图片成功加载。真实结果显示“研究达到时限，使用已有结果”。JSON 与 Markdown 使用同一选题数据。

本地 7 条历史结果逐条导出成功，读取前后原始 run.json 内容一致。测试资料保存在被 Git 忽略的 data-smoke，未混入用户历史。

## 可复查记录

- data-smoke/f2349f44-c785-4711-af2b-d09ca09bbb3c/run.json；每阶段 work 目录包含 prompt.txt、events.jsonl、searches.json、输出或检查点。

- data-smoke/1d00cb7b-c34d-40a5-a421-96bf3a038fca/run.json；每阶段 work 目录包含 prompt.txt、events.jsonl、searches.json、输出或检查点。



额度等外部错误从 CLI 的终止事件提取，避免被无关诊断日志遮盖。
